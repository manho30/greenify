name: Commit [Bot]🤖

on:

  push:
    branches:
      - master
      
  schedule:
  - cron: "*/15 * * * *" #adding cron job

  workflow_dispatch:

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2      
        with:
         persist-credentials: false
         fetch-depth: 0

      - name: Modify last update
        run: |
          d=`[Commit by bot] date '+%a %d %b %Y at %H:%M:%S'`
          echo $d > LAST_UPDATED
          
      - name: Commit changes
        run: |
          git config --local user.email "manhochan30@gmail.com"
          git config --local user.name "manho30"
          git add -A
          
          arr[0]="🤖[bot]💩 commit with success."
          arr[1]="🤖[bot]😽 commit with success."
          arr[2]="🤖[bot]🐸 commit with success."
          arr[3]="🤖[bot]🐰 commit with success."
          arr[4]="🤖[bot]🍉 commit with success."
          arr[5]="🤖[bot]🐼 commit with success."
          arr[6]="🤖[bot]🍗 commit with success."
          arr[7]="🤖[bot]🚀 commit with success."
          arr[8]="🤖[bot]🌝 commit with success."
          arr[9]="🤖[bot]⭐ commit with success."
          
          rand=$[$RANDOM % ${#arr[@]}]
          
          git commit -m "${arr[$rand]}"
          
      - name: GitHub Push
        uses: ad-m/github-push-action@v0.6.0
        with:
          force: true
          directory: "."
          github_token: ${{ secrets.GITHUB_TOKEN }}
